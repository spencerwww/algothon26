"""Local evaluator entry point for the reviewed standalone strategy.

For competition submission, rename ``updated_algorithm.py`` to the registered
team name. Keeping this wrapper tiny avoids maintaining two copies of the
strategy while allowing the repository's unchanged eval.py to run directly.
"""

from updated_algorithm import getMyPosition
